extends layout.pug

block content
    .container.text-center
        .row
            .col-sm
                .card
                    .card-header
                        h4 #{title || 'Something went wrong!'}
                    .card-body
                        | #{error || `Contact a staff if necessary`}
