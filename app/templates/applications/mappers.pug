extends ../layout.pug

block content
    form.container(action="/applications/mappers/store", method="post")
        .row
            .col.s12.center-align
                h4 Hi #{user.username} you're applying to be part of the team #{user.country.name} as a mapper

        .row
            .col.s12.center-align
                button.btn.waves-effect.waves-light(type="submit")
                    | Apply! 
                    i.material-icons.right send
